{
  "name": "mystiquill-app",
  "private": true,
  "scripts": {
    "dev": "next dev -p 3001",
    "build": "prisma generate --schema=prisma-mystiquill/schema.prisma && next build",
    "start": "next start -p 3001"
  },
  "prisma": {
    "schema": "prisma-mystiquill/schema.prisma"
  },
  "dependencies": {
    "slugify": "^1.6.6"
  }
}