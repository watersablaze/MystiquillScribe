{
  "name": "mystiquill-app",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "pnpm exec prisma generate --schema=prisma-mystiquill/schema.prisma && next build",
    "start": "next start"
  },
  "dependencies": {
    "slugify": "^1.6.6"
  }
}